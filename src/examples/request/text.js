import markup from '../../util/markup.js';

export default markup(
  '`m.request` performs an AJAX request against a specified url',
  'and returns a promise that resolves to the data fetched from',
  'the server.'
);
