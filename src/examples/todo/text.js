import markup from '../../util/markup.js';

export default markup(
  'This example is a mithril equivalent to the ',
  'TodoApp in the [react.js documentation](https://facebook.github.io/react/).'
);
